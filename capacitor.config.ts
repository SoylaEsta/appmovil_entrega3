import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'Gsepulvedah.Tellevo',
  appName: 'tellevo',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
